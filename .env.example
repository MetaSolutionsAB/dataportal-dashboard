# Serverspecifika inställningar för docker-compose.yml och docker-run.sh.
# Kopiera till .env (gitignorerad) och anpassa.

# Katalog på värden med exportfilerna, monteras som /data/exports i containern.
EXPORTS_DIR=/srv/exports
